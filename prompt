我正在开发一个基于flutter的word文件版本追踪的项目。你需要协助我。

采用Windows

查找文件时，如果搜索工具失败，可以用ls命令
在代码运行完毕后，没有必要用flutter run来测试有没有bug，除非我明确指示。

只要编译成功，就没有必要进行进一步的系统调试了，因为进一步的调试需要我用图形化界面。如果我有问题会和你说。

源代码文件夹是lib，下面有dart文件

不要用flutter  build和flutter clean，用flutter analyze来看有没有报错就行，不然会很没有效率

没有我的命令不要以任何形式启动代码，只能用analyze。而且必须要通过analyze来查看是否有报错。**需要同时对后端，也就是server和前端，也就是frontend进行analyze**

在生成完全部内容之后，请再生成一个git的commit message，用于显示本次更改的git的commit message。commit message用中文

服务器ip地址是47.242.109.145，3000端口的是git服务器端口，3920端口是另一个flask服务器的端口。但是本地的服务器端口是5000，那个是compare用的

edges文件定义了所有的需要渲染的合并边，其格式为，第一行为一堆零，后面的每行包含两个sha1，分别代表了两个commit节点之间有一条合并边

不要进行过分的修改，比如一次删除上千行之类的，更禁止清空或删除文件！！！

注意，我所指的git都不是当前项目的，而是位于APPdata/roaming/gitdocx/<projectname>下的

生成commit message，但不要执行实际的git commit。message的格式是：

feat/fix/refactor(<功能名称，比如frontend、server、push、pull>，可以多个)
1. 修改的小结
2. <修改的文件名>：<修改简介>
3. <修改的文件名>：<修改简介>
...

不要直接执行git的命令。

不要做任何我没有指定的修改，所有的修改的目的必须都是我提供给你的问题

在修改代码前需要检查是否在当前文档的其它地方已经有了相似的逻辑了，以免重复代码

如果我反馈了一个问题，可以尝试往里适度地加入调试信息，然后我会跟你说调试信息的输出内容

flutter analyze只能在你修改之后使用，不得在写代码过程中使用

当我询问是否有某个功能时，如果没有，那么就自动把他加上

在修改代码时，严禁把自己reasoning的内容加入到代码的注释中，注释应当尽可能地简洁，最好只保留必要注释

值得注意的是，如果我让你修改的只有cpp文件，那么你不需要执行flutter analyze，只需要用g++进行编译（编译出的exe文件和cpp文件同名且在同一个文件夹下），以下为g++的版本：

C:\Users\m1369>g++ --version
g++ (Rev8, Built by MSYS2 project) 15.2.0
Copyright (C) 2025 Free Software Foundation, Inc.
This is free software; see the source for copying conditions.  There is NO
warranty; not even for MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.

严禁擅自执行git diff，git add等命令

不要执行flutter pub add。在修改好之后告诉我在pubspec中要加什么就行

本项目有两个模式，文件夹模式（folder）和单独文件模式（solo）。在前者的模式中，我们管理的是一个文件夹中的所有文档的版本（每个文档分别管理），在后者中，我们只管理一个文档的版本。

在执行analyze后，只需要处理会导致编译报错的问题，其他的问题，比如deprecated或警告，不需要处理

为了调试错误，可以适当加入一些调试代码，我会把输出给你。

同时，任何错误都应当捕获并输出到控制台，方便我排查

本项目分为两个东西，一个叫“追踪对象”，另一个叫“存储地点”。其中追踪对象表示我们要追踪并设置为监控版本的对象，他可以是一个文档，或一个文件夹。另一个是gitdocx下的一个对应的东西，其用git仓库临时存储了这个文档的版本，其路径为APPDATA/gitdocx/<追踪项目名称>

对于复杂的逻辑，你可以生成一个测试脚本，但是对于简单的逻辑，就不用生成测试脚本了

git的位置在mingw64/bin/git.exe

如果我中断了dart analyze/flutter analyze的操作，不需要再执行一遍了，直接完成后面的工作即可

这是项目书：

问题引入
当我们需要管理文档版本时，我们通常会遇到这个情况：多个人需要同时管理word文档的版本的迭代。比如，如果我有一篇论文要写并且需要找两位导师进行修改。目前我们常用的做法是用微信来回传文件并修改标题以表明版本，比如《学术报告（我改）（导师A改）（删除xxx引用）》之类的。或者我们使用谷歌文档、金山文档等工具来实现多人实时编辑论文。
在第一种方法下，明显的不方便便是来回修改标题，而不同的人之间，有可能会因为缺乏相互沟通的信息而导致无法理解文档标题的一堆后缀导致修改出现错乱。在第二种方法下，虽然谷歌文档提供了相关历史记录的追寻功能，但是谷歌文档的历史记录是线性的，这导致多人如果同时修改一个文档会造成每个人和每个人的版本迭代混乱的问题。另一个不方便的地方是，如果我在9月10号发给我的导师A，并让他进行修改，导师A基于我发的版本又在10月12号改了一个版本，而导师B由于不知道导师A已经修改了版本，这导致导师B是基于我原来的版本而不是基于导师A的版本进行修改的，这导致出现了版本修改冲突的问题。
在另一些情况下，比如我们需要将同一段论文提交给多个比赛或报告，而不同的比赛有不同的字数和内容要求。目前的实践是备份一个共同的“论文基底”，然后再在一个基底上为适配每一个论文比赛或报告而做不同的论文修改。这种情况会导致管理多个文档而带来的混乱，同时还会出现文档误删的问题，以及这样子会导致当我在为某一个论文报告的特殊要求进行精调时，我可能会有一些新的想法需要提供给论文的基底，而这会导致版本管理的混乱。
我们的项目LambdaEssay可以解决上述的所有问题。我们使用了类似git的方式来管理word文档的版本。我们的项目集成了git的所有优势，包括在不同分支上的版本的迭代，分支的分叉和合并，本地到远程的推送和拉取。同时，它避免了直接使用git来单纯管理word文档的局限性：朴素地而言，word文档是二进制文件，每次版本迭代都会导致git对整个word文档进行一次复制，而这会导致许多内容被重复存储使得仓库膨胀。我们的项目可以只记录每个版本之间的word文档的真正修改的部分，不会对整个word文档进行重复存储。同时，git的操作繁多复杂，而且为控制台应用，操作起来不直观，无法看出不同分支之间的关系，对普通用户不友好。
术语定义
1.        追踪仓库
追踪仓库包含了所有的版本追踪所包含的东西，他是所有分支的集合。
2.        分支
一个分支可以被理解为一条文档版本的版本线路。他是一条从旧到新的线路
3.        提交节点
每一个提交节点表示一个word文档的版本。每一个提交节点都包含作者信息和备注信息
4.        分支头
一个分支的分支头是线路的最新的提交节点。
5.        提交
指在一个特定的分支上，将一个文档的版本作为一个提交节点，作为这个分支的下一个迭代。提交节点会放在分支头之后。
6.        本地仓库
用户存储在本地电脑上的追踪仓库
7.        远程仓库
在远程云端存储的追踪仓库
8.        分支合并
将两条分支进行合并，如果将A分支合并到B分支，那么就在B分支新建一个合并的提交节点，合并节点的具体内容取决于两条分支头部的文档信息。比如，如果A分支主要修改了文章的第二节，B分支主要修改了文章的第三节，那么A和B分支合并后就可以保留第二节和第三节。

在上图中，绿色分支合并到了蓝色分支。
在合并分支后两条分支可以继续向前进行版本迭代。分支合并的方法是，会利用word来计算两个待合并的分支的分支头部所对应的word文档的差异，然后由人工来决定保留双方的哪些信息。
9.        分支分叉
指将一个分支分为两条分支并分别进行版本迭代

在上图中，一条分支被分成了蓝色和绿色两条分支。
10.   推送
将本地的仓库推送到远程仓库并与其同步。
11.   拉取
用远程的仓库更新本地仓库并与其同步。
12.   冲突
如果本地的一条分支的版本为a→b→c，而远程的这条对应分支的版本为a→b→d。那么推送和拉取就会发生冲突。
13.   冲突-变基
假设本地的一条分支版本为a→b→c，远程对应的是a→b→d，那么在拉取冲突的情况下的变基之后，得到的分支是a→b→c→d；在推送冲突的情况下的变基之后，远程分支就会改为a→b→d→c。
14.   冲突-分叉
假设本地的一条分支版本为a→b→c，远程对应的是a→b→d。无论是本地还是远程，最终都会变为两条在b提交节点分叉的分支：
a→b→c
|→d
 
项目优势
本项目可以解决上述提出的问题。他可以通过不同分支来维护每一个文档作者的版本，并在适宜的位置通过分支合并来保留双方的更改。比如，在上述的导师的场景中，A导师可以先进行修改，并把修改推送到远程，随后B导师便会知道A导师所修改的分支，并在自己的分支上继续基于A导师的修改再进行修改，或者A和B导师同时修改，然后再进行合并。无需通过愚蠢的文件名修改来规定版本，无需把文件传来传去，无需使用谷歌文档来让历史记录是线性的。
谷歌文档的版本管理本质上只是一条巨大的分支，无法进行多分支的分叉和合并等操作。