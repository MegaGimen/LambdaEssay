# LambdaEssay Word版本追踪系统使用说明

LambdaEssay 是一个专为Word文档设计版本追踪工具，解决多人协作编辑时的版本混乱问题。它让你像程序员管理代码一样管理文档版本，但操作更加简单直观。

## 1. 核心概念

*   **追踪包**: 这是LambdaEssay使用的项目文件格式，是.tracking.zip文件。你可以把它看作一个“超级文件夹”，里面不仅存放着你的Word文档，还记录了文档所有的历史修改版本和分支信息。
*   **文件树**: 软件左侧的导航面板。在这里你可以浏览项目结构，支持像文件夹一样层层展开，管理复杂的嵌套文档。

## 2. 快速开始

### 启动与登录
启动软件后，使用账号登录即可连接云端服务，享受团队协作功能。离线模式下也可以管理本地版本。

### 开始工作
*   **新建项目**: 创建一个新的空白追踪包。
*   **打开项目**: 打开一个新的空白追踪包。

## 3. 主要功能

### 添加追踪包的文件

追踪包是一个追踪项目的整体，里面有一系列的被追踪版本的word文档

*   在左侧右击条目导入word文档进入项目追踪包并记录下这个word文档的外部路径。
*   可以导入一个文件夹，他会自动获得这个文件夹下的所有word文档并把整个文件夹导入进追踪包中。
*   双击右侧文件树的一个文档来打开这个文档的多分支版本历史。

### 可视化版本历史
软件中间的**版本图谱**是你查看历史的主要窗口：
*   每一个圆点代表一个历史版本（提交）。
*   双击圆点可以查看该版本的详细信息（谁在什么时候修改了什么），并且获得AI总结的该文档版本和上一个文档版本之间的差异。
*   每一个分支穿起了一些节点，也就是一个版本的链路。

### 文档编辑与提交
1.  **编辑**: 在word内部编辑文档
2.  **提交 (Commit)**: 点击界面上的“提交”按钮，填写修改说明。这相当于给当前的文档状态拍了一张“快照”，永久保存下来。
3.  **差异显示**：双击一个节点后，会显示这个节点的提交时间、提交信息等信息，并且会显示AI给出的这个版本与上个版本的差异以及行级差异。
4.  **回退**：双击一个节点后，在下方可以选择硬回退或软回退到这个版本。对于硬回退而言，他会强制把这个节点的版本更新到对应的word文档中，并且把这个分支的这个节点后的版本都清除掉；对于软回退而言，他会更新外部word文档，但是不会修改版本历史。两个操作都需慎重

### 分支管理
当你想尝试修改文档以适配不同的要求时，可以使用分支：
*   **创建分支**: 在某个版本节点上右键创建新分支。
*   **切换分支**: 在操作面板切换不同分支，文档不会自动变更为该分支对应的内容可放心切换。
*   **合并分支**: 将其他人的修改或另一条分支的内容合并到当前文档中。当合并时，系统会比较两个分支的最新的版本并生成差异的docx文档到对应的位置，差异部分用审阅标示。需要用户手动判断这些差异的部分应该相互保留哪些，完成后点击确认即可完成合并。

### 团队协作 (同步)

*   **推送 (Push)**: 将你的本地修改上传到服务器，分享给团队成员。如果本地的分支版本比远程落后，那么我们的系统为了阻止用落后的版本覆盖远程，会提示用户。建议用户在与其它的文档编辑者沟通后再执行强制推送。
*   **拉取 (Pull)**: 获取团队成员的最新修改。在拉取时，如果远程和本地的分支有冲突，那么会有三种冲突解决模式：历史合并、分叉和强制。就前者而言，他会把本地分支的修改保留，并在本地分支的顶端再加上远程的分支历史，也就是说最终的历史版本是远程的；就分叉而言，他会把远程作为一个新的分支分叉分出来，以便日后合并；如果是强制的话，那么他会直接覆盖本地的对应分支，需要谨慎操作。

### 项目备份

正如上述所言，本产品允许文档协作者进行强制推送，这具有一定的安全隐患，因为有可能出现恶意强制推送的情况，尤其是在法律合同等高风险、高保真性要求的情况下。因此，为了让用户的推送留痕，在服务器端，每次进行仓库推送时（除了第一次外），都会将推送后的仓库的状态打一次快照。同时，为了保证整个过程的高准确性，所打的每一次快照都会被计算一个哈希，每一次快照与每一次快照之间的哈希形成了一个线性的链条。我们将这个链条存在了区块链上并且允许你去查看。

你可以在打开一个追踪项目中的一个文件后，在右下角看见历史备份的按钮，点击它后就可以进入到历史备份页面，其中有每一个远程仓库的快照和此刻的本地仓库的比较。